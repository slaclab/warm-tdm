# Legacy local output directory

Older operations sessions may fall back here when launched from `software/scripts`.
New measurement notebooks use an explicit external run directory or ignored root
`runs/`; see [Notebook measurement runs](../../docs/notebook-runs.md).
Do not commit acquisitions or executed notebooks here.
